{
  "name": "surtur",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @surtur/frontend dev",
    "build": "pnpm -r build",
    "node:dev": "pnpm --filter @surtur/node dev",
    "typecheck": "pnpm -r typecheck"
  }
}
